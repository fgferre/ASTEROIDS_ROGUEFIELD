// Global Vitest setup executed once before the test suite runs.
// Imports the shared setup module so its afterEach hooks are registered
// automatically for every test file without manual imports.
import './setup.js';
